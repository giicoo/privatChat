import logging


logger = logging.getLogger(__name__)

def get_trust_color(tg_id: int) -> str:
    return "🟢"  # Placeholder

def save_message_to_db(user_data: dict, text: str, anonymous: bool) -> int:
    return 123  # Placeholder for saved message ID
